import React from 'react'

interface INotificationsConfiguration {
  //children: React.ReactNode
}

const NotificationsConfiguration: React.FunctionComponent<INotificationsConfiguration> = () => {
  return (
    <h1>
      notificacoes
    </h1>
  )
}

export default NotificationsConfiguration