import React from 'react'

interface IProfileConfiguration {
  //children: React.ReactNode
}

const ProfileConfiguration: React.FunctionComponent<IProfileConfiguration> = () => {
  return (
    <h1>
      profile
    </h1>
  )
}

export default ProfileConfiguration